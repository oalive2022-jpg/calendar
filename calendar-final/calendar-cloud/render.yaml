services:
  - type: web
    name: calendar-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: calendar-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
